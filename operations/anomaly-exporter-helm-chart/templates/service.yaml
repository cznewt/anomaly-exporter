apiVersion: v1
kind: Service
metadata:
  name: {{ include "anomaly-exporter.fullname" . }}
  labels:
    {{- include "anomaly-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "anomaly-exporter.selectorLabels" . | nindent 4 }}
